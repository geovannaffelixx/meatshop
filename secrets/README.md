# Credenciais locais

Coloque a conta de serviço Firebase Admin em `firebase-service-account.json`.
Esse arquivo é ignorado pelo Git e deve pertencer ao projeto `meatshop-3c78f`.

Inicie o ambiente com:

```powershell
.\scripts\start-local-with-firebase.ps1
```

Nunca compartilhe nem versione a chave privada.
