# ==========================================================
# 🌍 MEATSHOP BACKEND — ARQUIVO DE EXEMPLO (.env.example)
# ==========================================================
# Copie este arquivo e renomeie para ".env" antes de rodar o backend:
# Em seguida, edite os valores reais (senhas, segredos, URLs etc.)
# ==========================================================

# ----------------------------------------------------------
# ⚙️ AMBIENTE E APLICAÇÃO
# ----------------------------------------------------------
NODE_ENV=development
PORT=3001
TZ=America/Sao_Paulo

# ----------------------------------------------------------
# 🌐 FRONTEND
# ----------------------------------------------------------
# URL usada para permitir CORS e troca de cookies
FRONTEND_URL=http://localhost:3000

# ----------------------------------------------------------
# 🗄️ BANCO DE DADOS (PostgreSQL)
# ----------------------------------------------------------
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=sua_senha_aqui
DB_DATABASE=meatshop

# 🚫 NÃO use sincronização automática em produção!
# Use migrations para alterar o schema com segurança.
DB_SYNCHRONIZE=false

# ----------------------------------------------------------
# 🔐 AUTENTICAÇÃO — TOKENS JWT
# ----------------------------------------------------------
# Execute localmente o seguinte comando para gerar a chave segura:
# openssl rand -base64 64
JWT_SECRET=CHANGE-ME-ACCESS-64B
JWT_EXPIRATION=15m

# Refresh tokens (chave separada e expiração longa)
JWT_REFRESH_SECRET=CHANGE-ME-REFRESH-64B
JWT_REFRESH_EXPIRATION=7d

# ----------------------------------------------------------
# 🍪 COOKIES (HttpOnly)
# ----------------------------------------------------------
# ⚠️ Em produção, defina COOKIE_SECURE=true e configure HTTPS.
COOKIE_SECURE=false
COOKIE_SAMESITE=strict
COOKIE_DOMAIN=

# ----------------------------------------------------------
# 🧠 OUTRAS CONFIGURAÇÕES OPCIONAIS
# ----------------------------------------------------------
#LOG_LEVEL=info
#LOG_DIR=logs
